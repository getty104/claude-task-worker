# Coding Guidelines

このドキュメントは、過去のPRレビューで繰り返し指摘された観点をルール化したものです。
新規コード作成・既存コード修正時の自己レビューチェックリストとして参照してください。

## 更新ルール

- 同種の指摘が2回以上出現した場合のみルール化する
- ルールは「やるべきこと」を肯定形で書く
- 似た項目は統合し、ドキュメント全体のサイズを抑える
- 各ルールには代表的なPRコメントへのリンクを1〜2件添える

---

## 仕様と実装の整合

### 同じ契約を書く箇所は1つの正から導く

同じ値・同じ判定条件を複数箇所（`CLAUDE.md` / `docs/` / スキル本文 / テストの期待値 / スクリプト）へ書くときは、
実装のどの定数・関数が正かを確認し、そこへ揃える。片方だけが更新されると、後続の実装や運用が
誤った前提のまま進み、実装は正しいのにテストや手順書が失敗を見逃す状態になる。

具体的には、許可/拒否リストの列挙、API呼び出しのメソッド名と対応表、取得件数などのマジックナンバー、
テストの期待値を書くときに、対応する実装側の定義を必ず読んでから書く。

参考: [PR#266 Phase 1 の許可対象と `CLOUD_DENIED_WORKERS` の不一致](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136932), [PR#291 MCPメソッド指示が対応表と不一致](https://github.com/getty104/claude-task-worker/pull/291#discussion_r3880281502)

### 外部ツールの手順は使用バージョンの契約に合わせて書く

CLI・MCP・ライブラリの呼び出し手順をドキュメントやスキルへ書くときは、実際に使うバージョンの
公式ドキュメントまたは同梱仕様で、ツール名・引数・戻り値を確認してから書く。廃止済みのツール名や
用途違いのメソッド（例: ファイル取得にIssue用メソッドを渡す）を書くと、その手順は実行時に必ず失敗する。

参考: [PR#266 ファイル参照は `get_file_contents` へ](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3879332022), [PR#267 Pen CLI のAPI記述を現行契約へ](https://github.com/getty104/claude-task-worker/pull/267#discussion_r3878519654)

### 記述の断定範囲は実際に測定した範囲に限定する

実測結果をドキュメントへ書くときは、測定した条件で言えることだけを書く。複合条件で1回成功した
ことを各条件が単独で成立する根拠にしたり、ある環境での観測を別環境にも当てはめたりしない。
過大な一般化は、後続の実装がその記述を前提条件として扱うため、誤った設計判断へ直結する。

未測定の項目は「未測定」と明記するか、測定してから書く。

参考: [PR#266 T7 の結論を複合指定の受理に限定](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136916), [PR#266 未実測のまま独立と断定しない](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136905)

## エラー処理

### 失敗は必ず終了コード・戻り値へ伝播させる

シェルスクリプト・フック・インストーラで失敗を検出したら、そのまま終了コードへ反映する。
末尾の `exit 0`、`|| true`、失敗のログ出力のみといった握り潰しは、呼び出し元（CI・Stopフック・
ワーカー）が失敗を検知できず、「0件だったので何もしない」を正常完了として扱わせてしまう。

意図的に無視する失敗は、無視してよい理由（例: 削除対象が元から存在しない）を条件で絞り込んでから
無視する。すべての失敗を一律に無視しない。

参考: [PR#266 NG検出時に非ゼロ終了する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136937), [PR#266 ローカル実行時の終了コードを伝播する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878922758)

### 実行の前提は起動時に検証し、崩れていれば即座に失敗させる

処理の成立が暗黙の前提（必要なラベルの存在、プロンプト先頭のスラッシュコマンド、実測時と同じ構成）に
依存する場合は、その前提が満たされていることをコード側で検証し、満たされなければ起動時に失敗させる。
検証がないと前提が崩れても誰もエラーを出さず、タイムアウトまで空回りする静かな失敗になる。

ローカルから作成・判定できる前提は検証ではなく作成まで行い、実測で成立を確認した構成に依存する場合は、
その構成をテストで固定する（`includes` の包含判定ではなく、位置や並びまで固定する）。

参考: [PR#266 起動ガードが未作成ラベルを素通りする](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3886061518), [PR#314 スラッシュコマンドが本文先頭から外れる](https://github.com/getty104/claude-task-worker/pull/314#discussion_r3885711487)

## 入力の検証

### 外部から読む構造は形状と値まで検証する

設定ファイル・レジストリ・APIレスポンスなど自分で書いていないデータを読むときは、キーの
truthiness だけでなく、配列であること・要素が `null` でないこと・値が期待する型と内容であることまで
確認する。形状の仮定が外れると `find()` やプロパティ読み取りで例外になり、値の検証漏れは
誤った設定のまま起動を通過させる。

無効な形状では例外ではなく `undefined` などの安全な値を返し、非配列・`null` 要素・型違いを回帰テストに含める。

参考: [PR#266 プラグイン宣言の値を完全に検証する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136948), [PR#266 プラグインレジストリの形状を検証する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878922774)

## 非同期処理

### 完了判定は実際の終端状態を待って行う

投函コマンドの終了・中間状態の観測を、その先の非同期処理の完了と同一視しない。投函が成功した
時点で完了コールバックを呼ぶと、ラベル遷移や成果物検証が実作業の完了前に走り、状態が壊れる。
リモート実行やサブプロセスに対しては、終端状態を表すシグナル（明示的な完了ステータス・完了マーカー）を
定義し、それを待ってから完了処理へ進む。

参考: [PR#266 投函成功をクラウドタスク完了として扱わない](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3879927114), [PR#266 クラウド完了とローカル契約テストを分離する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3878136928)

### 常駐プロセスの処理は非同期にしタイムアウトと中断を付ける

単一イベントループ上で動く常駐プロセス（ワーカー・ディスパッチャー）のコールバック内では
`spawnSync` などの同期ブロッキング処理を使わず、`promisify` した `execFile` 等の非同期版を使う。
同期実行は同一プロセスの全ワーカーのポーリング・通知・シャットダウンを止め、ネットワーク待ちや
認証プロンプトで無期限に固まりうる。

外部コマンドには `timeout` / `killSignal` を付け、待機処理には `AbortSignal` を渡してシャットダウン時に
中断できるようにする。停止要求後に新たな副作用を起こさないよう、実行の直前にも停止状態を確認する。

参考: [PR#273 非同期コールバック内の `spawnSync`](https://github.com/getty104/claude-task-worker/pull/273#discussion_r3878743151), [PR#266 シャットダウンを全段階へ伝播する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3879927109)

## データ取得

### 一覧取得は既定の絞り込みを確認し全ページを走査する

GitHub API・MCP などの一覧取得は、既定で一部しか返さない。`state` 未指定で Open のみ、初期ページのみ、
レビュースレッドのみで通常コメントは含まない、といった既定の絞り込みを呼び出し前に確認し、
必要な範囲を明示的に指定する。取りこぼしは「該当なし」という誤った結論になり、重複作成や
対応漏れとして表面化する。

ページングは次ページがなくなるまで繰り返し、複数エンドポイントに分かれている場合は両方を取得して統合する。

参考: [PR#266 MCP のページング手順を追加する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3879332041), [PR#266 通常コメントは `get_comments` で取得する](https://github.com/getty104/claude-task-worker/pull/266#discussion_r3879332028)
